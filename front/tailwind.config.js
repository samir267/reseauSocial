/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: 'class', // or 'media' if you want it based on system settings

  content: [
    "./src/**/*.{js,jsx,ts,tsx}", // Adjust this if your files are in a different folder
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
